services:
    php:
        container_name: starter_php
        ports:
            - "8081:80"

    db:
        container_name: starter_db

networks:
    traefik_traefik_proxy:
        external: false
    starter_internal:
        driver: bridge
